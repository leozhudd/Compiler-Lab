declare void @memset(i32*, i32, i32)
declare i32 @getint()
declare void @putint(i32)
declare i32 @getch()
declare void @putch(i32)
declare i32 @getarray(i32*)
declare void @putarray(i32, i32*)

define dso_local i32 @foo(i32* %r1) {
    ret i32 1
}
define dso_local i32 @main() {
    %r2 = alloca [0 x i32]
    %r_for_memset3 = getelementptr [0 x i32], [0 x i32]* %r2, i32 0, i32 0
    call void @memset(i32* %r_for_memset3, i32 0, i32 0)
    %r5 = getelementptr [0 x i32], [0 x i32]* %r2, i32 0, i32 0
    %r4 = call i32 @foo(i32* %r5)
    ret i32 0
ret i32 0
}
